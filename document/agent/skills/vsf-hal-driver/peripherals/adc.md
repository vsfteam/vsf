# ADC

Template in `template/__series_name_a__/common/adc/`. Copy, rename placeholders, implement APIs via register access. Common inc: `adc_template.inc`, `adc_interface.c`, `adc_common.c`.

Reference: `driver/ST/STM32H7RSXX/common/` for multi-peripheral working examples.
