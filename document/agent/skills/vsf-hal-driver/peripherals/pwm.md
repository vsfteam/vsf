# PWM

Template in `template/__series_name_a__/common/pwm/`. Copy, rename placeholders, implement APIs via register access. Common inc: `pwm_template.inc`, `pwm_interface.c`, `pwm_common.c`.

Reference: `driver/ST/STM32H7RSXX/common/` for multi-peripheral working examples.
