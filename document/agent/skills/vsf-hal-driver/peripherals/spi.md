# SPI

Template in `template/__series_name_a__/common/spi/`. Copy, rename placeholders, implement APIs via register access. Common inc: `spi_template.inc`, `spi_interface.c`, `spi_common.c`.

Reference: `driver/ST/STM32H7RSXX/common/` for multi-peripheral working examples.
